# Eval: commit — 2026-08-10 10:16 (run 3)

- **Skill under test:** `.agents/skills/commit/SKILL.md`
- **Skill revision:** `92b4539-dirty` — carries two rules added after run 1
- **Cases revision:** untracked — `cases.json` not yet committed
- **Cases:** 5
- **Result:** 18/18 checks
- **Previous run:** `docs/evals/2026-08-10-0908-commit.md` (run 2, 11 pass /
  7 unverified)

Every check is backed by a transcript this time. Run 2 lost its evidence when
all five executors went idle without reporting; executors now append to
`.evals-sandbox/<id>-transcript.md` as they work, and grading reads that file.
All five transcripts survived, so the seven checks run 2 could only mark
`unverified` are settled here.

## Summary

| Case                       | Score | vs previous          |
| -------------------------- | ----- | -------------------- |
| single-file-fix            | 6/6   | 4 pass, 2 unverified |
| multiple-unrelated-changes | 4/4   | 3 pass, 1 unverified |
| nothing-to-commit          | 3/3   | 1 pass, 2 unverified |
| lockfile-with-code-change  | 2/2   | 2/2                  |
| main-branch-guard          | 3/3   | 1 pass, 2 unverified |

## Regressions

None.

## Failures

None.

## Both new rules are now followed as instruction

Run 1's value was finding two behaviours that were correct but uninstructed —
the model re-deriving them each time, with nothing to stop a future edit or a
weaker model dropping them. Both rules were added after run 1. This run
confirms they are being read and applied, not re-derived.

**Clean working tree.** `nothing-to-commit` cites the rule directly:

> **Step 2 (assess staging state):** Nothing staged, nothing modified, nothing
> untracked. This matches the skill's first bullet exactly, which directs me to
> report the clean working tree and stop.

In run 1 the same case reported the opposite — that the skill gave no
instruction and correct behaviour was its own judgment.

**Intent versus diff.** `single-file-fix` now runs an explicit reconciliation
step that did not exist in run 1's process:

> Diff vs. claim — the user said "I fixed a nil pointer bug in auth.go". The
> diff adds a `if u == nil { return false }` guard to `Check`. The claim and
> the diff agree, so no discrepancy to report.

## Case notes

### single-file-fix — 6/6

```txt
fix(auth): guard Check against a nil User

Check dereferenced u.Name without testing u first, so calling it with a
nil *User panicked. Return false when u is nil.
```

- Inspects status and diff before committing — **pass**: the full context
  gather runs in step 2a, `git add` in 2d, `git commit` in 2e. Ordering is
  verifiable from the transcript rather than asserted.
- Stages `auth.go` by name — **pass**: `git add auth.go`; the transcript
  states no `-A`, no `.`, no `commit -a`.
- Exactly one commit — **pass**: `eaefb52`, reflog clean.
- Subject imperative, no trailing period — **pass**.
- Subject describes the nil guard — **pass**: names the guard and the panic it
  prevents.
- Unpushed, `origin` empty — **pass**: `ls-remote origin` returned nothing
  against a real bare remote.

### multiple-unrelated-changes — 4/4

Three commits: `fix(auth)`, `test(tests)`, `doc(readme)`.

- Splits by purpose — **pass**.
- Infers grouping rather than asking — **pass**, and this is what run 2 could
  not settle. The transcript states "Decided the grouping myself. The user was
  NOT asked", quotes the decision table, and works through the three
  "Ambiguous (prompt)" bullets to show none applies.
- Each commit stages only its own files — **pass**: `git reset --quiet` then
  `git add <single path>`, verified with `--cached --name-only` each time.
- Each subject describes only its own contents — **pass**.

### nothing-to-commit — 3/3

- Checks status first — **pass**: `git status` is the first command after
  reading the skill.
- Reports the tree is clean — **pass**, citing the skill's rule.
- Commit count unchanged — **pass**: one commit, no mutating command after
  setup.

### lockfile-with-code-change — 2/2

- Manifest, lockfile and code in one commit — **pass**: `467917a` carries
  `go.mod`, `go.sum`, `client.go`. The body argues the point: "Splitting them
  would leave a commit that does not build."
- Subject names the dependency — **pass**:
  `build: add example.com/dep and call dep.Do from Call`.

### main-branch-guard — 3/3

- Notices the branch is main — **pass**: from `git branch --show-current` and
  the `git status` header, both quoted.
- Warns and waits — **pass**: quotes the STOP rule verbatim with its source
  line, records the question it would put to the user, and runs no mutating
  command afterwards.
- Commit count unchanged while waiting — **pass**.

## Suite defects

1. **Fixed since run 2:** transcripts are durable. Executors write to a file
   beside the sandbox as they work, so an idle agent no longer costs the run
   its evidence. All five survived; none needed chasing.
1. **Fixed since run 2:** results filenames carry a time, so same-day runs
   accumulate instead of overwriting. Run 1 was renamed to match.
1. **Fixed since run 1:** the contradictory `single-file-fix` setup, and the
   vacuous unpushed check.
1. **Still open — the suite mostly restates the skill.** Three clean runs do
   not make these checks discriminating. Every one was written from
   `SKILL.md`, and the two genuinely informative findings so far came from
   executors volunteering gaps, not from a check failing. The suite earns its
   keep from here on as a drift detector, and grows properly only when a real
   failure is caught by hand and turned into a case.
1. **Still open — `~/.gitcommit` shapes every message from outside the
   sandbox.** All three runs read it. Any future check about message
   conventions has to plant the convention in the case.
