{
  "allowNonWorkspaceAccess": true,
  "colorScheme": "solarized dark",
  "enableTelemetry": false,
  "gcp": {
    "project": "{{ op://Employee/Google VertexAI Project ID/password }}",
    "location": "global"
  },
  "model": "Gemini 3.1 Pro (High)",
  "permissions": {
    "allow": [
      "command(agy)",
      "command(cat)",
      "command(colima)",
      "command(diff)",
      "command(docker ps)",
      "command(docker pull)",
      "command(docker)",
      "command(echo)",
      "command(file)",
      "command(find)",
      "command(git add)",
      "command(git branch --show-current 2>/dev/null)",
      "command(git describe)",
      "command(git diff --cached 2>/dev/null)",
      "command(git diff --stat --cached 2>/dev/null)",
      "command(git diff --stat HEAD 2>/dev/null)",
      "command(git diff 2>/dev/null)",
      "command(git diff)",
      "command(git grep)",
      "command(git log -5 --oneline 2>/dev/null)",
      "command(git log)",
      "command(git ls-files)",
      "command(git ls-tree)",
      "command(git merge-base)",
      "command(git rev-parse)",
      "command(git show)",
      "command(git status 2>/dev/null)",
      "command(git status)",
      "command(git symbolic-ref)",
      "command(go doc)",
      "command(gofumpt)",
      "command(grep)",
      "command(head)",
      "command(hexdump)",
      "command(ls)",
      "command(lsof)",
      "command(make deps-update)",
      "command(make fmt)",
      "command(make help)",
      "command(make lint-all)",
      "command(make lint-govet)",
      "command(make lint-staticcheck)",
      "command(make local-config)",
      "command(make redis-start)",
      "command(make test)",
      "command(make test-all)",
      "command(make test-integration)",
      "command(make tests)",
      "command(openssl)",
      "command(ps)",
      "command(sed)",
      "command(wc)",
      "mcp(google-workspace/*)"
    ]
  },
  "statusLine": {
    "type": "command",
    "command": "~/.gemini/antigravity-cli/statusline.sh",
    "enabled": true
  },
  "trustedWorkspaces": [
    "/Users/mmcdonnell/code/agent-skills",
    "/Users/mmcdonnell/code/fastly/blue-ribbon"
  ]
}
